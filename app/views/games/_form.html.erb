<% if @game.errors.any? %>
	
	<div class='errors'>
		<p>There are <%= pluralize(@game.errors.count, 'error') %> that need fixing:</p>
		<ul>
			<% @game.errors.full_messages.each do |e| %>
				<li><%= e %></li>
			<% end %>
		</ul>
	</div>

<% end %>

<%= form_for @game do |f| %>

	<table>
		<tr>
			<th><%= f.label :name %></th>
			<td><%= f.text_field :name %></td>
		</tr>
		<tr>
			<th><%= f.label :genre %></th>
			<td><%= f.select :genre, ["Shooter", "RPG", "Strategy"] %></td>
		</tr>
		<tr>
			<th><%= f.label :publisher %></th>
			<td><%= f.text_field :publisher %></td>
		</tr>
		<tr>
			<th><%= f.label :platform %></th>
			<td><%= f.select :platform, ["PC", "Mac", "iOS", "Android", "Console"] %></td>
		</tr>
		<tr>
			<td><%= f.submit @game.new_record? ? 'Create Game' : 'Update Game' %></td>
		</tr>
	</table>

<% end %>